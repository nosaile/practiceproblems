"use strict";


function findSubstring(str){

    switch(str){
        case(str):
            str = str.replaceAll('the cat is fat', '')
            break;
    }

    console.log(str)
}

findSubstring("the cat is fat and the dog is skinny");
findSubstring("dog cat dog the cat is fat cat dog the is the cat is dog")